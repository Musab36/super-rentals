{{! Component for showing, hiding images and deleting rentals}}
<li>
  {{rental.owner}}'s {{rental.type}} in {{rental.city}}
  {{#if isImageShowing }}
    <p><img src={{rental.image}} alt={{rental.type}} {{action 'imageHide'}}></p>
  {{else}}
    <button class="button" {{action 'imageShow'}}>Show image</button>
  {{/if}}
 {{update-rental rental=rental update="update"}} {{! Update component}}
  <button {{action 'delete' rental}}>Delete Rental</button>
</li>
